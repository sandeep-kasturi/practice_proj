server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sandeep-kasturi/config-server/tree/main
          clone-on-start: true

eureka:
  client:
    serviceUrl:
      defaultZone: http:localhost:8761/eureka/


